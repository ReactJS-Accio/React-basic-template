import "./styles.css";

const Navbar = () => (
  <div className="navbar">
    <h1>Page Title</h1>
    <div className="links">
      <a href="#">Home</a>
      <a href="#">Blog</a>
      <a href="#">Contact</a>
      <a href="#">About Us</a>
    </div>
  </div>
);

export default Navbar;
